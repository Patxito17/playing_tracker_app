# Archivos de debug — no son builds de producción
app-debug*

# Checksums generados por Flutter (no útiles para distribución)
*.sha1

# Logs y metadatos internos del proceso de exportación de Xcode
*.log
DistributionSummary.plist
ExportOptions.plist
